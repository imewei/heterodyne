# Divergence Registry — Heterodyne → Homodyne Strict 1:1 Parity

Records every gap from `REPORT.md` that was dispositioned `DROP` in `DISPOSITIONS.md`. Each entry names the divergence and the one-line rationale.

Populated incrementally by Phase 4 PRs as they encounter DROP-dispositioned rows. Use this file as the authoritative source of "what is intentionally different between heterodyne and homodyne" for future audit re-runs.

## D1 — Physics-exempt files (5 rows)

- file_inventory:missing_py_file | `optimization.nlsq.shear_weighting` | Layer 5 (shear-sensitivity weighting); not applicable to heterodyne's velocity-phase physics model (D1, spec §2)

## D2 — Physics-specific docs (10 rows)

_Populated by Phase 4 PR 10._

## D3 — Parameter-content / shear-physics divergence (~148 rows)

_Populated incrementally by Phase 4 PRs 1–11 as each PR encounters D3-matching rows in its scope. Each entry: `category:kind | qualname | rationale (one of: Layer-5 shear-only / homodyne-only param / homodyne-only physics class / homodyne-only CLI mode)`._

- file_inventory:extra_py_file | `optimization.nlsq.strategies.chunked` | heterodyne-only ChunkedStrategy class (memory-bounded NLSQ chunking); no homodyne equivalent. KEEP (D3 heterodyne-required). Phase 4 PR 1 Task 1.2 Step 2.5 verification classified the chunked.py↔chunking.py pair as "different files" — homodyne's chunking.py contains stratification utility functions (analyze_angle_distribution, StratificationDiagnostics, create_angle_stratified_data, etc.) deferred to a separate port task (not in scope for PR 1 Task 1.2 as originally written).
- file_inventory:extra_py_file | `optimization.nlsq.strategies.base` | heterodyne-only strategy dispatch abstraction (`StrategyResult` dataclass, `FittingStrategy` Protocol, `select_strategy` function, `_estimate_available_memory`). Referenced by 11 source files: every strategy class (`chunked`, `jit_strategy`, `residual`, `residual_jit`, `sequential`, `hybrid_streaming`, `out_of_core`, `stratified_ls`), `executors.py`, `strategies/__init__.py`, and `nlsq/__init__.py`. Homodyne removed its equivalent `selection.py` in v2.12.0 (note: "Use NLSQ's WorkflowSelector instead") and replaced `FittingStrategy` with a raw-Callable ABC interface (`OptimizationExecutor.execute(residual_fn, xdata, ydata, ...)`). Migrating heterodyne to homodyne's pattern would require restructuring all 11 source files and 2 test files with no structural target to migrate to. KEEP (D3, heterodyne architectural choice). Phase 4 PR 1 Task 1.3.
- file_inventory:extra_py_file | `optimization.nlsq.strategies.jit_strategy` | heterodyne-only `JITStrategy` class: JAX-JIT-compiled full Jacobian + residual fitting strategy with XLA compilation-cache key logging, covariance estimation from the Jacobian (`_estimate_covariance_from_jac`), compile-time/run-time accounting, and a `dogbox→trf` coercion guard. Referenced from 10 distinct files: `strategies/__init__.py`, `strategies/base.py` (`select_strategy` default), `strategies/executors.py` (lazy import), `strategies/residual.py` (docstring cross-reference), `strategies/residual_jit.py` (docstring comparison), `strategies/sequential.py` (strategy dispatch map), `nlsq/__init__.py` (public re-export), and 3 test files (`test_strategies_nlsq.py`, `test_strategies_base.py`, `test_no_scipy.py`). Homodyne has no `jit_strategy.py` in its strategies directory (homodyne strategies: `residual_jit`, `executors`, `stratified_ls`, `out_of_core`, `residual`, `chunking`, `sequential`, `hybrid_streaming`). KEEP (D3, heterodyne architectural choice — JIT strategy is the primary high-performance path for the heterodyne two-path integral model). Phase 4 PR 1 Task 1.4.
- file_inventory:extra_py_file | `optimization.nlsq.validation.bounds` | heterodyne-only `BoundsValidator` class: validates fitted parameters against the heterodyne 14-parameter `DEFAULT_REGISTRY` (hard bounds check + soft edge-fraction proximity warning). Imports `DEFAULT_REGISTRY` directly and issues severity-graduated `ValidationReport` entries. Homodyne has no `bounds.py` in its validation subpackage (homodyne validation: `result_validator.py`, `fit_quality.py`, `input_validator.py`). Only 2 distinct import sites outside the file itself, both `__init__.py` re-exports (`validation/__init__.py` and `nlsq/__init__.py`), but the functionality is inseparable from heterodyne's parameter registry. KEEP (D3, heterodyne-required — bounds contract tied to the 14-param registry absent from homodyne). Phase 4 PR 1 Task 1.4.
- file_inventory:extra_py_file | `optimization.nlsq.validation.convergence` | heterodyne-only `ConvergenceValidator` class: assesses NLSQ optimizer convergence quality (termination status, iteration-limit detection, outlier-residual ratio check). Imports base types (`ValidationIssue`, `ValidationReport`, `ValidationSeverity`) from `validation/result.py`. Homodyne has no `convergence.py` in its validation subpackage; convergence checks are folded into `result_validator.py` as individual `validate_*` functions without a dedicated class. Only 2 distinct import sites outside the file itself, both `__init__.py` re-exports. KEEP (D3, heterodyne architectural choice — separate convergence class retained from heterodyne's original validation architecture). Phase 4 PR 1 Task 1.4.
- file_inventory:extra_py_file | `optimization.nlsq.validation.result` | heterodyne-only base-type module: defines `ValidationSeverity` (Enum), `ValidationIssue` (dataclass), `ValidationReport` (dataclass with `.errors`/`.warnings`/`.summary` helpers), and the class-based `ResultValidator`. These base types are imported by `validation/bounds.py`, `validation/convergence.py`, and `validation/__init__.py`. Heterodyne additionally has `validation/result_validator.py` (the homodyne-parity functional API introduced in the NLSQ parity fixes of 2026-05-11). Homodyne's `result_validator.py` defines its own local `ValidationReport` dataclass inline and exports `validate_covariance`, `validate_optimized_params`, `validate_result_consistency` as standalone functions — it has no separate base-type module. The heterodyne `result.py` cannot be collapsed into `result_validator.py` without also migrating `bounds.py` and `convergence.py` (both D3 KEEP above), which would require dissolving those two files. KEEP (D3, foundation dependency for three D3-kept files; elimination requires a coordinated multi-file restructure deferred to a future parity PR). Phase 4 PR 1 Task 1.4.
- classes:missing_method | `optimization.nlsq.anti_degeneracy_controller.AntiDegeneracyController.get_shear_weights` | Layer 5 method; heterodyne has no shear term in the g2 formula (D3, spec §2). Phase 4 PR 2 Task 2.4.
- classes:missing_method | `optimization.nlsq.anti_degeneracy_controller.AntiDegeneracyController.update_shear_phi0` | Layer 5 method; heterodyne has no shear term in the g2 formula (D3, spec §2). Phase 4 PR 2 Task 2.4.
- classes:missing_method | `optimization.nlsq.anti_degeneracy_controller.AntiDegeneracyController.use_shear_weighting` | Layer 5 property; always False in heterodyne — no shear physics (D3, spec §2). Phase 4 PR 2 Task 2.4.
- classes:missing_field  | `optimization.nlsq.anti_degeneracy_controller.AntiDegeneracyController.shear_weighter` | Layer 5 field; heterodyne has no shear term in the g2 formula (D3, spec §2). Phase 4 PR 2 Task 2.4.
- missing_method | `optimization.nlsq.adapter.NLSQAdapter.fit` kwarg `shear_transforms` | Layer-5 shear-only; heterodyne has no shear sinc term in the g2 formula, so this kwarg is omitted from the ported `fit()` signature. `analysis_mode` default changed from homodyne's `'static_isotropic'` to `'full'` — heterodyne always uses its 14-parameter full model. Phase 4 PR 3 Batch 1.

- changed:signature | `optimization.nlsq.jacobian.analyze_parameter_sensitivity` | Heterodyne takes `(jacobian: np.ndarray, param_names)` (pre-computed Jacobian); homodyne takes `(residual_fn, x_subset, params, param_names)`. Heterodyne's callers all pass the Jacobian array directly (11 test sites). Migrating to the residual_fn interface would break the existing compute_numerical_jacobian + compare_jacobians toolchain. KEEP (D3, heterodyne architectural choice — Jacobian-first API). Phase 4 PR 3 Batch 4.
- changed:signature | `optimization.nlsq.jacobian.compute_jacobian_condition_number` | Heterodyne takes `(jacobian: np.ndarray) -> float`; homodyne takes `(residual_fn, x_subset, params) -> float | None`. Same rationale as analyze_parameter_sensitivity above — pre-computed Jacobian API used by all regression tests. KEEP (D3, heterodyne architectural choice). Phase 4 PR 3 Batch 4.
- changed:signature | `optimization.nlsq.jacobian.estimate_gradient_noise` | Heterodyne takes `(jacobian, residuals) -> dict[str, float]`; homodyne takes `(residual_fn, x_subset, params, n_samples, perturbation, seed) -> float | None`. Heterodyne's richer return type (mean/max ratio + noisy fraction) is a superset. KEEP (D3, heterodyne architectural choice). Phase 4 PR 3 Batch 4.
- missing_in_heterodyne | `optimization.nlsq.fit_computation.compute_g2_batch` | Homodyne's g2 uses `(t1, t2, L)` two-time matrix with sample-to-detector distance; heterodyne uses `(t, q, dt)` single time array — not portable. Heterodyne equivalent is `compute_c2_batch`. KEEP (D3, physics model difference — two-path integral architecture). Phase 4 PR 3 Batch 4.
- missing_in_heterodyne | `optimization.nlsq.fit_computation.compute_g2_batch_with_per_angle_scaling` | Same physics mismatch as compute_g2_batch above; heterodyne equivalent is `compute_c2_batch_with_per_angle_scaling`. KEEP (D3, physics model difference). Phase 4 PR 3 Batch 4.
- changed:backend | `optimization.nlsq.cmaes_wrapper.CMAESWrapper.fit` (NLSQ evosax backend) | Homodyne's `CMAESWrapper.fit` uses NLSQ's `CMAESOptimizer` (evosax JAX-accelerated evolution). Heterodyne uses the `cma` library backend instead — evosax is not required for CPU-only heterodyne and adds a heavy optional dependency. The public signature is identical (`model_func, xdata, ydata, p0, bounds, sigma, warmstart_chi2 → CMAESResult`). KEEP (D3, heterodyne architectural choice — CPU-only, cma backend). Phase 4 PR 3 Batch 5.
- changed:signature | `optimization.nlsq.fallback_chain.execute_optimization_with_fallback` | Homodyne: `(strategy, wrapped_residual_fn, xdata, ydata, validated_params, nlsq_bounds, loss_name, x_scale_value, config, start_time, log, enable_recovery, execute_with_recovery_fn, fit_with_hybrid_streaming_fn, streaming_available, curve_fit_fn, curve_fit_large_fn, fast_mode) → tuple[...]`. Heterodyne: `(model, c2_data, phi_angle, config, *, start_strategy, weights) → NLSQResult`. Heterodyne passes the HeterodyneModel directly and internalises all strategy dispatch; homodyne passes every callable explicitly. Migrating to homodyne's interface would require restructuring the entire NLSQ pipeline. KEEP (D3, heterodyne architectural choice — model-first API). Phase 4 PR 3 Batch 5.
- changed:signature | `optimization.nlsq.fallback_chain.get_fallback_strategy` | Homodyne: `(current_strategy) → OptimizationStrategy | None` (no error param). Heterodyne: `(current, error=None) → OptimizationStrategy | None` adds error-heuristic for memory-aware escalation. Heterodyne signature is a strict superset. KEEP (D3, heterodyne architectural choice — richer fallback heuristics). Phase 4 PR 3 Batch 5.
- changed:signature | `optimization.nlsq.fallback_chain.handle_nlsq_result` | Homodyne: `(result, strategy) → tuple[np.ndarray, np.ndarray, dict]` (pcov always ndarray, strategy logged). Heterodyne: `(raw_result) → tuple[np.ndarray, np.ndarray | None, dict]` (pcov may be None, no strategy arg). Heterodyne's looser pcov handling is intentional — NLSQ may not produce a covariance for all backends. KEEP (D3, heterodyne architectural choice). Phase 4 PR 3 Batch 5.
- changed:signature | `optimization.nlsq.recovery.execute_with_recovery` | Homodyne: `(residual_fn, xdata, ydata, initial_params, bounds, strategy, log, loss_name, x_scale_value, handle_nlsq_result_fn, curve_fit_fn, curve_fit_large_fn) → tuple[...]` passes all callables explicitly. Heterodyne: `(fit_fn, initial_params, bounds, config, *, max_retries, perturb_scale, rng_seed) → NLSQResult` wraps a single fit_fn. KEEP (D3, heterodyne architectural choice — simpler retry API). Phase 4 PR 3 Batch 5.
- changed:signature | `optimization.nlsq.recovery.diagnose_error` | Homodyne: `(error, params, bounds, attempt) → dict[str, Any]` returns rich dict with `error_type`, `suggestions`, `recovery_strategy.new_params`. Heterodyne: `(error) → ErrorDiagnosis` returns frozen dataclass with `category`, `message`, `recoverable`, `suggested_action`. Heterodyne's typed dataclass is architecturally superior for static analysis. KEEP (D3, heterodyne architectural choice — typed API). Phase 4 PR 3 Batch 5.
- changed:signature | `optimization.nlsq.recovery.safe_uncertainties_from_pcov` | Homodyne: `(pcov: np.ndarray, n_params: int) → np.ndarray` (pcov mandatory). Heterodyne: `(pcov: np.ndarray | None, n_params: int = 14) → np.ndarray` allows None and has physics-aware default of 14 params. Heterodyne is a strict superset. KEEP (D3, heterodyne architectural choice — richer API). Phase 4 PR 3 Batch 5.
- changed:signature | `optimization.nlsq.parallel_accumulator.accumulate_chunks_sequential` | Homodyne: `(chunks: list[tuple[JtJ, Jtr, chi2]]) → tuple[JtJ, Jtr, chi2, count]` reduces pre-computed (JtJ, Jtr, chi2) tuples. Heterodyne: `(chunks, residual_fn, params) → GaussNewtonAccumulation` computes residuals+jacobian on-the-fly from raw data chunks. Heterodyne's interface matches the two-path integral compute model; homodyne's interface expects pre-computed accumulator tuples from JIT kernels. Migrating would require restructuring all OOC callers. KEEP (D3, heterodyne architectural choice — compute-on-demand vs reduce-precomputed). Phase 4 PR 3 Batch 6.
- changed:signature | `optimization.nlsq.parallel_accumulator.accumulate_chunks_parallel` | Same semantic divergence as accumulate_chunks_sequential above — homodyne takes `(chunks: list[tuple[JtJ, Jtr, chi2]], n_workers)`, heterodyne takes `(chunks, residual_fn, params, n_workers) → GaussNewtonAccumulation`. KEEP (D3, heterodyne architectural choice). Phase 4 PR 3 Batch 6.
- changed:signature | `optimization.nlsq.parallel_accumulator.create_ooc_kernels` | Homodyne: `(per_angle_scaling, n_phi, phi_unique, t1_unique_global, t2_unique_global, n_t1, n_t2, q_val, L_val, dt_val) → tuple[Callable, Callable]` creates physics-specific JIT kernels for the homodyne two-time g2 model. Heterodyne: `(n_params: int = 14) → tuple[Callable, Callable]` creates generic J^T J and J^T r kernels. The homodyne signature is tied to homodyne's `compute_g2_scaled` physics function and the (phi, t1, t2) data layout — not portable to heterodyne's (t, q, dt) model. KEEP (D3, physics model difference — two-path integral architecture). Phase 4 PR 3 Batch 6.
- changed:signature | `optimization.nlsq.parallel_accumulator.should_use_parallel_accumulation` | Homodyne: `(n_chunks: int) → bool` (no threshold param). Heterodyne: `(n_chunks: int, threshold: int = 10) → bool` (threshold is configurable). Heterodyne is a strict superset. KEEP (D3, heterodyne architectural choice — richer API). Phase 4 PR 3 Batch 6.
- extra_config_key | `optimization.nlsq.parallel_accumulator.GaussNewtonAccumulation.JtJ` | Heterodyne-only dataclass field; homodyne uses raw tuples `(JtJ, Jtr, chi2)` not a named dataclass. KEEP (D3, heterodyne architectural choice — typed accumulator). Phase 4 PR 3 Batch 6.
- extra_config_key | `optimization.nlsq.parallel_accumulator.GaussNewtonAccumulation.Jtf` | Same rationale as GaussNewtonAccumulation.JtJ above. KEEP (D3). Phase 4 PR 3 Batch 6.
- extra_config_key | `optimization.nlsq.parallel_accumulator.GaussNewtonAccumulation.cost` | Same rationale. KEEP (D3). Phase 4 PR 3 Batch 6.
- extra_config_key | `optimization.nlsq.parallel_accumulator.GaussNewtonAccumulation.n_data` | Same rationale. KEEP (D3). Phase 4 PR 3 Batch 6.
- missing_field | `optimization.nlsq.parameter_index_mapper.ParameterIndexMapper.n_phi` | Homodyne's `ParameterIndexMapper` is a dataclass with constructor `(n_phi, n_physical, fourier, use_constant)` — the Fourier/constant scaling index-mapping utility for the 7-parameter homodyne model. Heterodyne's `ParameterIndexMapper` has a fundamentally different constructor `(varying_names, varying_full_indices, log_mask)` — it is a bidirectional index mapper between the full 14-param space and the varying-param optimizer space. Adding homodyne's `n_phi` field would require a constructor-signature change that breaks `ParameterManager` and all 9 call sites in heterodyne. KEEP (D3, heterodyne architectural choice — constructor API preserved per batch 6 D3 note). Phase 4 PR 3 Batch 6.
- missing_field | `optimization.nlsq.parameter_index_mapper.ParameterIndexMapper.n_physical` | Same rationale as n_phi above — tied to homodyne's 7-parameter model; incompatible with heterodyne's `varying_names` constructor. KEEP (D3). Phase 4 PR 3 Batch 6.
- missing_field | `optimization.nlsq.parameter_index_mapper.ParameterIndexMapper.fourier` | Same rationale — homodyne's FourierReparameterizer reference; heterodyne uses a completely different constructor. KEEP (D3). Phase 4 PR 3 Batch 6.
- missing_field | `optimization.nlsq.parameter_index_mapper.ParameterIndexMapper.use_constant` | Same rationale — homodyne's constant-scaling flag; not applicable to heterodyne's `varying_names` constructor. KEEP (D3). Phase 4 PR 3 Batch 6.
- missing_method | `optimization.nlsq.parameter_index_mapper.ParameterIndexMapper.get_group_indices` | Returns `[(contrast_start, contrast_end), (offset_start, offset_end)]` for homodyne's contrast/offset per-angle structure. Heterodyne has no contrast/offset group structure — its parameter space is the 14-param heterodyne model, not grouped by angle scaling. Method semantics are not portable. KEEP (D3, homodyne-only per-angle scaling structure). Phase 4 PR 3 Batch 6.
- missing_method | `optimization.nlsq.parameter_index_mapper.ParameterIndexMapper.get_per_angle_indices` | Returns indices of per-angle (contrast + offset) parameters in homodyne's parameter vector. Heterodyne has no contrast/offset per-angle indices — not applicable. KEEP (D3). Phase 4 PR 3 Batch 6.
- missing_method | `optimization.nlsq.parameter_index_mapper.ParameterIndexMapper.get_physical_indices` | Returns indices of homodyne's 7 physical parameters. Heterodyne's `ParameterIndexMapper` already has `varying_full_indices` and `fixed_full_indices` which serve this purpose for the 14-parameter model. Semantics differ. KEEP (D3). Phase 4 PR 3 Batch 6.
- missing_method | `optimization.nlsq.parameter_index_mapper.ParameterIndexMapper.get_covariance_slice_indices` | Returns `(slice, slice)` for homodyne's contrast vs offset covariance blocks. Heterodyne has no contrast/offset block structure in covariance. KEEP (D3, homodyne-only per-angle scaling structure). Phase 4 PR 3 Batch 6.
- missing_method | `optimization.nlsq.parameter_index_mapper.ParameterIndexMapper.mode_name` | Returns `"constant"`, `"fourier"`, or `"individual"` for homodyne's per-angle scaling modes. Heterodyne has no such scaling modes in its ParameterIndexMapper — mode selection lives in `config.py` (`per_angle_mode`). KEEP (D3). Phase 4 PR 3 Batch 6.
- missing_method | `optimization.nlsq.parameter_index_mapper.ParameterIndexMapper.n_per_angle_total` | Counts total per-angle parameters (Fourier coefficients or raw per-phi count) for homodyne. Not applicable to heterodyne's `varying_names`-based mapper. KEEP (D3). Phase 4 PR 3 Batch 6.
- missing_method | `optimization.nlsq.parameter_index_mapper.ParameterIndexMapper.n_per_group` | Counts parameters per group (contrast or offset) for homodyne. Not applicable. KEEP (D3). Phase 4 PR 3 Batch 6.
- missing_method | `optimization.nlsq.parameter_index_mapper.ParameterIndexMapper.total_params` | Returns `n_per_angle_total + n_physical` for homodyne. Heterodyne has `n_full` (always 14) and `n_varying` — different semantics. KEEP (D3). Phase 4 PR 3 Batch 6.
- missing_method | `optimization.nlsq.parameter_index_mapper.ParameterIndexMapper.use_fourier` | Property returning whether Fourier reparameterization is active for homodyne. Heterodyne's ParameterIndexMapper is not connected to Fourier reparameterization. KEEP (D3). Phase 4 PR 3 Batch 6.
- missing_method | `optimization.nlsq.parameter_index_mapper.ParameterIndexMapper.validate_indices` | Validates that group indices are within homodyne's parameter vector length. Heterodyne already has `full_to_varying` / `varying_to_full` with proper bounds checking — different validation model. KEEP (D3). Phase 4 PR 3 Batch 6.
- missing_method | `optimization.nlsq.parameter_index_mapper.ParameterIndexMapper.get_diagnostics` | Returns dict with `mode_name`, `use_constant`, `use_fourier`, `n_phi`, `n_physical` for homodyne. All fields are homodyne-only concepts. Heterodyne already has `get_debug_info()` equivalent via `varying_names`/`varying_full_indices` properties. KEEP (D3). Phase 4 PR 3 Batch 6.
- missing_config_key | `optimization.nlsq.parameter_index_mapper.ParameterIndexMapper.fourier` | Same as missing_field entry above — homodyne dataclass field, not applicable. KEEP (D3). Phase 4 PR 3 Batch 6.
- missing_config_key | `optimization.nlsq.parameter_index_mapper.ParameterIndexMapper.n_phi` | Same as missing_field entry above. KEEP (D3). Phase 4 PR 3 Batch 6.
- missing_config_key | `optimization.nlsq.parameter_index_mapper.ParameterIndexMapper.n_physical` | Same as missing_field entry above. KEEP (D3). Phase 4 PR 3 Batch 6.
- missing_config_key | `optimization.nlsq.parameter_index_mapper.ParameterIndexMapper.use_constant` | Same as missing_field entry above. KEEP (D3). Phase 4 PR 3 Batch 6.

## Manual-narrative-diff escalations (3 rows; from REPORT.md §"Manual narrative diffs")

_Populated as the corresponding PRs land:_

- (PR 5) `optimization.cmc.sampler.run_nuts_with_retry` ported from homodyne — **not a divergence**, fixed in scope. Registry entry will be deleted when PR 5 lands.
- (PR 4) `optimization.cmc.warmstart.clamp_params_to_interior` + `clamp_to_interior` signatures preserved during absorption into `priors.py` — **not a divergence**, fixed in scope. Registry entry will be deleted when PR 4 lands.

---

## How to add an entry

When a Phase 4 PR encounters a DROP-dispositioned row, append a line under the matching D1/D2/D3 section:

```
- {category}:{kind} | `{qualname or path}` | {one-line rationale}
```

Example:
```
- file_inventory:missing_py_file | `core.homodyne_model` | physics wrapper analog; heterodyne has core.heterodyne_model (D1)
- file_inventory:extra_py_file   | `core.physics_kernel` | heterodyne-only physics formula body, spec §2 exempt (D1)
```
